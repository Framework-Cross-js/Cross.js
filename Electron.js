// Framework Electron
const string = class String{};
let val = new string();
val.value = new String();
let stringObj = {
  return: val
};
let ctx = stringObj;
const message = String;
 message.prototype = 'Promise';
if (message === undefined) {
 const Error_1 = new SyntaxError().message = 'Electron message not loaded';
  throw this;
  throw Error_1;
};
 //yield
String.prototype(ctx);
const log = 'log';
const int = (e) => {
  export class Electro {
    static message(message) {
     console.log(message)
    } 
  };
};
const Logger = int();
console.info(Logger).catch(console.error.bind(console));

function  isawl(text, Console){
  function OptionsParametr(any){
    Array = { Options : new Option() } 
  };
  this.text = stringObj;
  this.Options = OptionsParametr();
  this.Console = Console;
  if(this.Console === true || Console === true ){
Electro.log('ElectronScript container!⚡')
  };
};
isawl('container', true );

export class Electron {
  static message(message) {
     console.log(message)
  } 
  static launch(){
    console.info('launch');
     console.message('launch work.');
  }
  static config(){
    function launchTableConfig(){
      console.table(tabularData)
    }
  }
  launchTableConfig();
}

let Microsoft;
Microsoft = {
 Object = {
    'soft': 'Microsoft',
    'Copyright' : '2021-2022'
  }
}

let tabularData = {
  "type" : "Cross lang progress programm",
  "language" : "Electron", 
  "soft" : Microsoft 
}

// function automatic work 
var promise = new Promise(function(resolve, reject) {
console.info(tabularData)
});

